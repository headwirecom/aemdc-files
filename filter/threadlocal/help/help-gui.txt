Create a ThreadLocal filter and service

This is a OGSi service that is a Sling Filter which sets ThreadLocal state
AND an OSGi Service that exposes the ThreadLocal state to other OSGi Services.

Provide placeholders as needed. Default values will be used derived from the 'Target Name'.